# Reference implementations (legacy PHP code - read-only, not tracked)
reference/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
pulldb.egg-info/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
ENV/
env/
.venv/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.project
.pydevproject

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/
*.cover
.hypothesis/

# Logs
*.log
logs/

# Security scan reports (timestamped, regenerate on demand)
security-scan-*.json

# Temporary files and working directories
tmp/
temp/
*.tmp
*.bak
*.backup

# Database files (test databases, dumps, backups)
*.sql
!docs/atomic_rename_procedure.sql
!schema/pulldb/*.sql
!schema/pulldb_service/*.sql
!schema/archived/pulldb.sql
!pulldb/template_after_sql/**/*.sql
*.sql.gz
*.sql.zst
*.tar
*.tar.gz
*.tar.zst
*.dump
*.dmp
test_db/
test_data/

# Debian packages (build artifacts)
*.deb

# Vendor binaries (re-download from upstream release archives; never tracked)
pulldb/binaries/*
!pulldb/binaries/myloader-0*
!pulldb/binaries/zstd-local
!pulldb/binaries/zstd-local/**

# OS
.DS_Store
Thumbs.db
*.lnk

# Local configuration overrides (if needed)
local.conf
.env
.env.local
venv/

# Runtime / analysis caches
.mypy_cache/
.ruff_cache/

# Benchmarking / profiling artifacts
.benchmarks/
profile/
profiling/

# Daemon / process artifacts
*.pid
*.out
*.trace
*.stackdump

# pullDB working/extraction directories (never versioned)
pulldb_work_*/
pulldb_extract_*/
pulldb_tmp_*/
tmp_pulldb_*/

# Test environment (local development only)
test-env/

# Session notes (local context, not tracked)
.session-context.md

# Generated CSS (runtime theme manifests, regenerated on startup)
pulldb/web/static/css/generated/

# NOTE: Do NOT ignore business SQL scripts in customers_after_sql/ or qa_template_after_sql/
.backup-config/
.venv-test/
