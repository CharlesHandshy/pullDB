#!/usr/bin/env bash
# Dev helper (review-only)
set -euo pipefail
echo "Using AWS profile: pr-dev"
# export AWS_PROFILE=pr-dev
# aws sts get-caller-identity
# aws secretsmanager get-secret-value --secret-id /pulldb/mysql/coordination-db
